<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>RapiQR — India's #1 Emergency QR Safety Platform</title>
<meta name="description" content="Instant QR safety for your family, vehicles & home. No app needed. Zero subscription. Scan-to-connect, without ever exposing your number.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600;700;800&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
<style>
  :root{
    --bg:#0a0b0e;
    --surface:#14161b;
    --surface-2:#1b1e25;
    --surface-3:#22262f;
    --line:rgba(255,255,255,0.08);
    --line-strong:rgba(255,255,255,0.16);
    --text:#edeef0;
    --text-dim:#9098a6;
    --text-faint:#5c6472;
    --brass:#c9a227;
    --brass-bright:#e8c860;
    --brass-dim:rgba(201,162,39,0.12);
    --alert:#e5544a;
    --alert-dim:rgba(229,84,74,0.14);
    --safe:#5fd88f;
    --radius:16px;
    --maxw:1220px;
  }
  *{margin:0;padding:0;box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  @media (prefers-reduced-motion: reduce){
    html{scroll-behavior:auto;}
    *{animation-duration:0.001ms !important; animation-iteration-count:1 !important; transition-duration:0.001ms !important;}
  }
  body{
    background:var(--bg); color:var(--text); font-family:'Inter',sans-serif;
    font-size:16px; line-height:1.6; -webkit-font-smoothing:antialiased; overflow-x:hidden;
  }
  h1,h2,h3,h4{font-family:'Space Grotesk',sans-serif; font-weight:600; letter-spacing:-0.01em;}
  .mono{font-family:'IBM Plex Mono',monospace;}
  a{color:inherit; text-decoration:none;}
  img,svg{display:block; max-width:100%;}
  button{font-family:inherit; cursor:pointer; background:none; border:none; color:inherit;}
  ul{list-style:none;}
  .wrap{max-width:var(--maxw); margin:0 auto; padding:0 32px;}
  @media (max-width:640px){ .wrap{padding:0 20px;} }
  :focus-visible{outline:2px solid var(--brass-bright); outline-offset:3px; border-radius:4px;}

  /* Buttons */
  .btn{display:inline-flex; align-items:center; justify-content:center; gap:8px; padding:14px 28px; border-radius:999px; font-weight:600; font-size:14.5px; letter-spacing:0.01em; border:1px solid transparent; transition:transform .22s ease, box-shadow .22s ease, border-color .22s ease, color .22s ease; white-space:nowrap;}
  .btn-primary{ background:linear-gradient(180deg, var(--brass-bright), var(--brass)); color:#12100a; }
  .btn-primary:hover{ transform:translateY(-1px); box-shadow:0 10px 26px rgba(201,162,39,0.3); }
  .btn-ghost{ background:transparent; border-color:var(--line-strong); color:var(--text); }
  .btn-ghost:hover{ border-color:var(--brass); color:var(--brass-bright); }
  .btn-alert{ background:linear-gradient(180deg,#f16c62,var(--alert)); color:#1a0705; }
  .btn-alert:hover{ transform:translateY(-1px); box-shadow:0 10px 24px rgba(229,84,74,0.32); }
  .btn-sm{ padding:10px 20px; font-size:13.5px; }
  .btn-block{ width:100%; }

  /* Nav */
  header{ position:sticky; top:0; z-index:200; background:rgba(10,11,14,0.72); backdrop-filter:blur(14px) saturate(140%); -webkit-backdrop-filter:blur(14px) saturate(140%); border-bottom:1px solid var(--line); }
  nav{ display:flex; align-items:center; justify-content:space-between; height:74px; gap:20px; }
  .brand{ display:flex; align-items:center; gap:9px; font-family:'Space Grotesk',sans-serif; font-weight:700; font-size:18px; letter-spacing:0.01em; flex-shrink:0; }
  .brand-mark{ width:28px; height:28px; }
  .nav-links{ display:flex; align-items:center; gap:32px; font-size:14px; color:var(--text-dim); }
  .nav-links a{ transition:color .2s ease; }
  .nav-links a:hover{ color:var(--text); }
  .nav-actions{ display:flex; align-items:center; gap:12px; flex-shrink:0; }
  .nav-toggle{ display:none; }
  @media (max-width: 980px){ .nav-links{ display:none; } }
  @media (max-width: 560px){ .nav-actions .btn-ghost{ display:none; } }

  /* Aurora backdrop */
  .aurora{ position:fixed; inset:0; z-index:0; pointer-events:none; overflow:hidden; }
  .aurora span{ position:absolute; border-radius:50%; filter:blur(90px); opacity:0.16; }
  .aurora .a1{ width:520px; height:520px; background:var(--brass); top:-180px; left:-120px; animation:drift1 26s ease-in-out infinite alternate; }
  .aurora .a2{ width:420px; height:420px; background:var(--alert); bottom:-160px; right:-100px; opacity:0.10; animation:drift2 30s ease-in-out infinite alternate; }
  @keyframes drift1{ from{ transform:translate(0,0);} to{ transform:translate(60px,40px);} }
  @keyframes drift2{ from{ transform:translate(0,0);} to{ transform:translate(-50px,-30px);} }

  /* Hero */
  .hero{ position:relative; padding:88px 0 70px; z-index:1; }
  .hero-grid{ display:grid; grid-template-columns:1.08fr 0.92fr; gap:48px; align-items:center; }
  @media (max-width:980px){ .hero-grid{ grid-template-columns:1fr; } }
  .eyebrow{ display:inline-flex; align-items:center; gap:9px; font-family:'IBM Plex Mono',monospace; font-size:12px; letter-spacing:0.13em; color:var(--brass-bright); text-transform:uppercase; margin-bottom:20px; }
  .eyebrow::before{ content:""; width:7px; height:7px; border-radius:50%; background:var(--brass-bright); box-shadow:0 0 0 4px var(--brass-dim); }
  .hero h1{ font-size:clamp(2.3rem, 4.4vw, 3.7rem); line-height:1.08; margin-bottom:20px; }
  .hero h1 em{ font-style:normal; color:var(--brass-bright); }
  .hero p.lead{ font-size:17.5px; color:var(--text-dim); max-width:470px; margin-bottom:30px; }
  .hero-ctas{ display:flex; gap:14px; flex-wrap:wrap; margin-bottom:36px; }
  .trust-row{ display:flex; align-items:center; gap:14px; }
  .avatars{ display:flex; }
  .avatars span{ width:34px; height:34px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-family:'IBM Plex Mono',monospace; font-size:11px; font-weight:600; color:#12100a; background:var(--brass-bright); border:2px solid var(--bg); margin-left:-9px; }
  .avatars span:first-child{ margin-left:0; }
  .avatars span:nth-child(2){ background:#8fb4ff; }
  .avatars span:nth-child(3){ background:#8fe0c2; }
  .avatars span:nth-child(4){ background:#f0a8a2; }
  .trust-row p{ font-size:13.5px; color:var(--text-faint); }
  .trust-row strong{ color:var(--text-dim); }

  /* Hero visual: sticker + phone mockup */
  .hero-visual{ position:relative; display:flex; align-items:center; justify-content:center; min-height:460px; }
  .sticker-card{
    position:relative; width:250px; background:linear-gradient(165deg,#f4f0e6,#e9e2d0); border-radius:20px; padding:20px;
    box-shadow:0 30px 60px -20px rgba(0,0,0,0.55), 0 0 0 1px rgba(0,0,0,0.06);
    transform:rotate(-7deg) translateX(-30px);
    transition:transform .4s ease;
  }
  .hero-visual:hover .sticker-card{ transform:rotate(-4deg) translateX(-30px) translateY(-4px); }
  .sticker-top{ display:flex; align-items:center; justify-content:space-between; margin-bottom:14px; }
  .sticker-brand{ font-family:'Space Grotesk',sans-serif; font-weight:700; font-size:13px; color:#12100a; display:flex; align-items:center; gap:6px; }
  .sticker-brand svg{ width:16px; height:16px; }
  .sticker-tag{ font-family:'IBM Plex Mono',monospace; font-size:8.5px; letter-spacing:0.08em; color:#7a6f4f; background:rgba(201,162,39,0.18); padding:3px 7px; border-radius:5px; }
  .sticker-qr{ background:#fff; border-radius:10px; padding:10px; display:flex; align-items:center; justify-content:center; }
  .sticker-qr img, .sticker-qr canvas{ width:100% !important; height:auto !important; image-rendering:pixelated; }
  .sticker-plate{ margin-top:12px; text-align:center; }
  .sticker-plate .id{ font-family:'IBM Plex Mono',monospace; font-weight:600; font-size:15px; color:#12100a; letter-spacing:0.03em; }
  .sticker-plate .sub{ font-family:'IBM Plex Mono',monospace; font-size:9.5px; color:#7a7360; margin-top:3px; letter-spacing:0.04em; }

  .phone-mockup{
    position:absolute; right:-10px; bottom:-14px; width:210px; background:var(--surface); border:1px solid var(--line-strong);
    border-radius:26px; padding:14px 14px 18px; box-shadow:0 30px 70px -24px rgba(0,0,0,0.65);
    transform:rotate(6deg) translateX(20px);
    transition:transform .4s ease;
  }
  .hero-visual:hover .phone-mockup{ transform:rotate(3deg) translateX(20px) translateY(-6px); }
  .phone-notch{ width:44px; height:5px; background:var(--surface-3); border-radius:4px; margin:0 auto 12px; }
  .phone-label{ font-family:'IBM Plex Mono',monospace; font-size:9px; color:var(--text-faint); letter-spacing:0.06em; text-transform:uppercase; margin-bottom:8px; display:flex; align-items:center; gap:5px; }
  .phone-label::before{ content:"📷"; font-size:10px; }
  .phone-screen{ background:var(--bg); border:1px solid var(--line); border-radius:14px; padding:14px 12px; }
  .phone-screen .car{ font-family:'IBM Plex Mono',monospace; font-size:11px; color:var(--text-dim); margin-bottom:2px; }
  .phone-screen .status{ font-size:10px; color:var(--safe); margin-bottom:12px; }
  .phone-btn{ width:100%; text-align:left; padding:9px 10px; border-radius:9px; background:var(--surface-2); border:1px solid var(--line); font-size:11px; margin-bottom:7px; color:var(--text); display:flex; align-items:center; gap:7px; }
  .phone-btn.alert{ background:var(--alert-dim); border-color:rgba(229,84,74,0.3); color:#ff9b93; }

  .badge-float{ position:absolute; background:var(--surface); border:1px solid var(--line-strong); border-radius:999px; padding:8px 14px; font-family:'IBM Plex Mono',monospace; font-size:11.5px; color:var(--text-dim); display:flex; align-items:center; gap:7px; box-shadow:0 12px 26px rgba(0,0,0,0.45); animation:floatY 4.5s ease-in-out infinite; }
  .badge-float.speed{ top:8px; left:0; }
  .badge-float.speed span{ color:var(--brass-bright); font-weight:600; }
  @keyframes floatY{ 0%,100%{ transform:translateY(0);} 50%{ transform:translateY(-8px);} }

  /* Stats band */
  .stats-band{ border-top:1px solid var(--line); border-bottom:1px solid var(--line); background:var(--surface); position:relative; z-index:1; }
  .stats-grid{ display:grid; grid-template-columns:repeat(3,1fr); }
  .stat-cell{ padding:34px 24px; text-align:center; border-right:1px solid var(--line); }
  .stat-cell:last-child{ border-right:none; }
  @media (max-width:640px){ .stat-cell{ padding:24px 12px; } }
  .stat-cell .icon{ font-size:22px; margin-bottom:8px; }
  .stat-cell .num{ font-family:'Space Grotesk',sans-serif; font-size:clamp(1.6rem,3vw,2.2rem); color:var(--brass-bright); }
  .stat-cell .label{ font-size:12.5px; color:var(--text-faint); margin-top:4px; letter-spacing:0.02em; }

  section{ position:relative; padding:104px 0; z-index:1; }
  @media (max-width:640px){ section{ padding:72px 0; } }
  .section-head{ max-width:640px; margin-bottom:52px; }
  .section-head.center{ margin-left:auto; margin-right:auto; text-align:center; }
  .section-head h2{ font-size:clamp(1.9rem, 3.1vw, 2.5rem); margin-bottom:14px; }
  .section-head p{ color:var(--text-dim); font-size:16.5px; }
  .divider{ height:1px; background:var(--line); }

  .reveal{ opacity:0; transform:translateY(18px); transition:opacity .6s ease, transform .6s ease; }
  .reveal.in{ opacity:1; transform:translateY(0); }

  /* Products */
  .filter-row{ display:flex; gap:10px; margin-bottom:36px; flex-wrap:wrap; }
  .filter-pill{ padding:9px 18px; border-radius:999px; border:1px solid var(--line-strong); font-size:13.5px; color:var(--text-dim); transition:all .2s ease; }
  .filter-pill:hover{ color:var(--text); border-color:var(--brass); }
  .filter-pill.active{ background:var(--brass-bright); color:#12100a; border-color:var(--brass-bright); font-weight:600; }
  .product-grid{ display:grid; grid-template-columns:repeat(3,1fr); gap:22px; }
  @media (max-width:980px){ .product-grid{ grid-template-columns:repeat(2,1fr); } }
  @media (max-width:680px){ .product-grid{ grid-template-columns:1fr; } }
  .product-card{ background:var(--surface); border:1px solid var(--line); border-radius:var(--radius); padding:26px; display:flex; flex-direction:column; transition:border-color .25s ease, transform .25s ease; }
  .product-card:hover{ border-color:var(--line-strong); transform:translateY(-3px); }
  .product-card.hide{ display:none; }
  .product-top{ display:flex; justify-content:space-between; align-items:flex-start; margin-bottom:14px; }
  .product-badge{ font-family:'IBM Plex Mono',monospace; font-size:10px; letter-spacing:0.06em; text-transform:uppercase; background:var(--brass-dim); color:var(--brass-bright); padding:4px 10px; border-radius:999px; }
  .product-rating{ font-size:12.5px; color:var(--text-faint); display:flex; align-items:center; gap:4px; }
  .product-rating .stars{ color:var(--brass-bright); }
  .product-card h3{ font-size:18px; margin-bottom:8px; }
  .product-card > p.desc{ color:var(--text-dim); font-size:13.5px; margin-bottom:16px; }
  .product-card ul{ display:flex; flex-direction:column; gap:9px; margin-bottom:20px; flex:1; }
  .product-card li{ display:flex; gap:8px; font-size:13px; color:var(--text-dim); align-items:flex-start; }
  .product-card li svg{ width:14px; height:14px; color:var(--safe); flex-shrink:0; margin-top:3px; }
  .product-price-row{ display:flex; align-items:baseline; gap:9px; margin-bottom:2px; }
  .product-price-row .now{ font-family:'Space Grotesk',sans-serif; font-size:22px; color:var(--text); }
  .product-price-row .was{ font-size:13.5px; color:var(--text-faint); text-decoration:line-through; }
  .product-price-row .off{ font-size:11px; color:var(--safe); font-weight:600; }
  .product-card .lifetime{ font-size:11.5px; color:var(--text-faint); margin-bottom:16px; }
  .product-card .btn{ margin-top:auto; }

  /* How it works */
  .steps-row{ display:grid; grid-template-columns:repeat(5,1fr); gap:0; border:1px solid var(--line); border-radius:var(--radius); overflow:hidden; }
  @media (max-width:980px){ .steps-row{ grid-template-columns:1fr; } }
  .step{ padding:32px 22px; border-right:1px solid var(--line); position:relative; transition:background .25s ease; }
  .step:last-child{ border-right:none; }
  @media (max-width:980px){ .step{ border-right:none; border-bottom:1px solid var(--line); } .step:last-child{ border-bottom:none; } }
  .step.active{ background:linear-gradient(180deg, var(--surface-2), var(--surface)); }
  .step .idx{ width:30px; height:30px; border-radius:50%; border:1px solid var(--line-strong); display:flex; align-items:center; justify-content:center; font-family:'IBM Plex Mono',monospace; font-size:12.5px; color:var(--text-dim); margin-bottom:18px; }
  .step.active .idx{ background:var(--brass-bright); color:#12100a; border-color:var(--brass-bright); }
  .step h3{ font-size:15.5px; margin-bottom:8px; }
  .step p{ color:var(--text-dim); font-size:13px; }
  .step .live{ display:inline-block; margin-top:10px; font-family:'IBM Plex Mono',monospace; font-size:9.5px; letter-spacing:0.06em; color:var(--brass-bright); background:var(--brass-dim); padding:3px 8px; border-radius:999px; }

  /* Use cases */
  .usecase-tabs{ display:flex; gap:8px; flex-wrap:wrap; margin-bottom:36px; border-bottom:1px solid var(--line); padding-bottom:0; }
  .usecase-tab{ padding:12px 4px; margin-right:26px; font-size:14.5px; color:var(--text-faint); border-bottom:2px solid transparent; transition:color .2s ease, border-color .2s ease; }
  .usecase-tab:hover{ color:var(--text-dim); }
  .usecase-tab.active{ color:var(--text); border-color:var(--brass-bright); }
  .usecase-panel{ display:none; }
  .usecase-panel.active{ display:grid; grid-template-columns:1fr 1fr; gap:44px; align-items:center; animation:fadeIn .45s ease; }
  @keyframes fadeIn{ from{ opacity:0; transform:translateY(8px);} to{ opacity:1; transform:translateY(0);} }
  @media (max-width:820px){ .usecase-panel.active{ grid-template-columns:1fr; } }
  .usecase-panel h3{ font-size:24px; margin-bottom:18px; }
  .usecase-panel ul{ display:flex; flex-direction:column; gap:14px; margin-bottom:26px; }
  .usecase-panel li{ display:flex; gap:12px; font-size:14.5px; color:var(--text-dim); align-items:flex-start; }
  .usecase-panel li svg{ width:18px; height:18px; color:var(--brass-bright); flex-shrink:0; margin-top:2px; }
  .usecase-visual{ background:var(--surface); border:1px solid var(--line); border-radius:var(--radius); padding:36px; display:flex; align-items:center; justify-content:center; min-height:220px; }
  .usecase-visual .glyph{ width:88px; height:88px; color:var(--brass-bright); opacity:0.9; }

  /* Feature grid */
  .feature-grid{ display:grid; grid-template-columns:repeat(4,1fr); gap:1px; background:var(--line); border:1px solid var(--line); border-radius:var(--radius); overflow:hidden; }
  @media (max-width:980px){ .feature-grid{ grid-template-columns:repeat(2,1fr); } }
  @media (max-width:560px){ .feature-grid{ grid-template-columns:1fr; } }
  .feature{ background:var(--surface); padding:28px 24px; transition:background .25s ease; }
  .feature:hover{ background:var(--surface-2); }
  .feature .glyph{ width:24px; height:24px; color:var(--brass-bright); margin-bottom:16px; }
  .feature h3{ font-size:15px; margin-bottom:8px; }
  .feature p{ color:var(--text-dim); font-size:13px; }

  /* Interactive scanner demo */
  .demo-shell{ display:grid; grid-template-columns:0.85fr 1.15fr; gap:56px; align-items:center; }
  @media (max-width:900px){ .demo-shell{ grid-template-columns:1fr; } }
  .demo-panel h3{ font-size:24px; margin-bottom:14px; transition:opacity .2s ease; }
  .demo-panel p{ color:var(--text-dim); font-size:15px; margin-bottom:28px; min-height:75px; transition:opacity .2s ease; }
  .demo-stepper{ display:flex; gap:0; margin-bottom:28px; }
  .demo-stepper .dstep{ flex:1; padding-bottom:12px; border-bottom:2px solid var(--line); font-family:'IBM Plex Mono',monospace; font-size:11px; color:var(--text-faint); letter-spacing:0.04em; transition:all .3s ease; }
  .demo-stepper .dstep.active{ color:var(--brass-bright); border-color:var(--brass-bright); }
  .demo-panel-actions{ display:flex; gap:12px; }

  .demo-phone{ justify-self:center; width:300px; background:linear-gradient(180deg,#1a1d24,#101216); border:1px solid var(--line-strong); border-radius:36px; padding:18px; box-shadow:0 40px 90px -30px rgba(0,0,0,0.7); }
  .demo-phone .notch{ width:60px; height:6px; background:#000; border-radius:6px; margin:0 auto 16px; }
  .demo-screen{ background:var(--bg); border-radius:20px; padding:22px 18px; min-height:400px; position:relative; overflow:hidden; }
  .demo-state{ display:none; animation:fadeIn .35s ease; }
  .demo-state.active{ display:block; }
  .demo-back{ font-family:'IBM Plex Mono',monospace; font-size:11.5px; color:var(--text-faint); margin-bottom:16px; display:inline-flex; align-items:center; gap:5px; }
  .demo-back:hover{ color:var(--text-dim); }
  .demo-tag-id{ font-family:'IBM Plex Mono',monospace; font-size:13px; color:var(--text-dim); }
  .demo-tag-sub{ font-size:11px; color:var(--text-faint); margin-bottom:20px; }
  .demo-cta-label{ font-family:'IBM Plex Mono',monospace; font-size:10px; color:var(--text-faint); letter-spacing:0.08em; margin-bottom:12px; }
  .demo-action{ width:100%; text-align:left; padding:14px 14px; border-radius:12px; background:var(--surface-2); border:1px solid var(--line); font-size:13.5px; margin-bottom:10px; display:flex; align-items:center; gap:10px; transition:background .2s ease, border-color .2s ease; color: var(--text);}
  .demo-action:hover{ background:var(--surface-3); border-color:var(--line-strong); }
  .demo-action.emergency{ background:var(--alert-dim); border-color:rgba(229,84,74,0.3); color:#ff9b93; }
  .demo-action .ic{ font-size:16px; }

  .result-icon{ width:52px; height:52px; border-radius:50%; background:var(--brass-dim); display:flex; align-items:center; justify-content:center; font-size:22px; margin-bottom:16px; }
  .result-icon.safe{ background:rgba(95,216,143,0.14); }
  .result-title{ font-size:17px; margin-bottom:4px; }
  .result-sub{ font-size:12.5px; color:var(--text-faint); margin-bottom:20px; }
  .result-line{ display:flex; align-items:center; gap:10px; background:var(--surface-2); border:1px solid var(--line); border-radius:10px; padding:12px 14px; margin-bottom:10px; font-size:13px; }
  .result-line .rt{ color:var(--text); }
  .result-line .rs{ font-size:11px; color:var(--text-faint); display:block; }
  .demo-next{ width:100%; margin-top:14px; }
  .emg-grid{ display:grid; grid-template-columns:1fr 1fr; gap:8px; margin-bottom:16px; }
  .emg-item{ padding:12px 10px; border-radius:10px; background:var(--surface-2); border:1px solid var(--line); font-size:12px; text-align:center; }
  .emg-caption{ font-size:11.5px; color:var(--text-faint); text-align:center; }

  /* Pricing */
  .pricing-grid{ display:grid; grid-template-columns:repeat(3,1fr); gap:24px; align-items:stretch; }
  @media (max-width:900px){ .pricing-grid{ grid-template-columns:1fr; } }
  .price-card{ background:var(--surface); border:1px solid var(--line); border-radius:var(--radius); padding:34px 28px; display:flex; flex-direction:column; }
  .price-card.featured{ border-color:var(--brass); background:linear-gradient(180deg, var(--surface-2), var(--surface)); position:relative; }
  .price-card.featured::before{ content:"MOST POPULAR"; position:absolute; top:-1px; right:22px; transform:translateY(-50%); background:var(--brass-bright); color:#12100a; font-family:'IBM Plex Mono',monospace; font-size:10px; letter-spacing:0.07em; padding:5px 11px; border-radius:999px; font-weight:600; }
  .price-card h3{ font-size:18px; margin-bottom:8px; }
  .price-card .price-sub{ color:var(--text-dim); font-size:13.5px; margin-bottom:22px; line-height:1.5; }
  .price-card .amount{ font-family:'Space Grotesk',sans-serif; font-size:34px; margin-bottom:4px; }
  .price-card .amount span{ font-size:15px; color:var(--text-faint); font-family:'Inter',sans-serif; text-decoration:line-through; margin-left:6px;}
  .price-card .per{ color:var(--text-faint); font-size:12.5px; margin-bottom:26px; }
  .price-card ul{ display:flex; flex-direction:column; gap:12px; margin-bottom:28px; flex:1; }
  .price-card li{ display:flex; gap:9px; font-size:13.5px; color:var(--text-dim); align-items:flex-start; }
  .price-card li svg{ width:15px; height:15px; color:var(--brass-bright); flex-shrink:0; margin-top:2px; }

  /* Distributor */
  .distributor-band{ background:var(--surface); border-top:1px solid var(--line); border-bottom:1px solid var(--line); }
  .dist-benefits{ display:grid; grid-template-columns:repeat(4,1fr); gap:20px; margin-bottom:56px; }
  @media (max-width:900px){ .dist-benefits{ grid-template-columns:repeat(2,1fr); } }
  @media (max-width:560px){ .dist-benefits{ grid-template-columns:1fr; } }
  .dist-card{ background:var(--surface-2); border:1px solid var(--line); border-radius:var(--radius); padding:24px; }
  .dist-card .num{ font-family:'IBM Plex Mono',monospace; font-size:12px; color:var(--brass-bright); margin-bottom:12px; }
  .dist-card h4{ font-size:15px; margin-bottom:8px; font-family:'Space Grotesk',sans-serif; font-weight:600; }
  .dist-card p{ font-size:13px; color:var(--text-dim); }
  .kit-grid{ display:grid; grid-template-columns:1fr 1fr; gap:24px; }
  @media (max-width:820px){ .kit-grid{ grid-template-columns:1fr; } }
  .kit-card{ background:var(--surface-2); border:1px solid var(--line); border-radius:var(--radius); padding:32px; }
  .kit-card.highlight{ border-color:var(--brass); }
  .kit-tag{ font-family:'IBM Plex Mono',monospace; font-size:10.5px; letter-spacing:0.06em; text-transform:uppercase; color:var(--brass-bright); background:var(--brass-dim); padding:4px 10px; border-radius:999px; display:inline-block; margin-bottom:16px; }
  .kit-card h3{ font-size:20px; margin-bottom:6px; }
  .kit-card .kit-sub{ font-size:13.5px; color:var(--text-dim); margin-bottom:18px; }
  .kit-card .kit-price{ font-family:'IBM Plex Mono',monospace; font-size:15px; color:var(--text); margin-bottom:18px; }
  .kit-card ul{ display:flex; flex-direction:column; gap:10px; margin-bottom:24px; }
  .kit-card li{ font-size:13.5px; color:var(--text-dim); }
  .dist-contact{ text-align:center; margin-top:40px; font-size:13.5px; color:var(--text-faint); }
  .dist-contact a{ color:var(--brass-bright); }

  /* Testimonials */
  .testi-note{ font-family:'IBM Plex Mono',monospace; font-size:11.5px; color:var(--text-faint); margin-bottom:30px; }
  .testi-grid{ display:grid; grid-template-columns:repeat(2,1fr); gap:22px; }
  @media (max-width:820px){ .testi-grid{ grid-template-columns:1fr; } }
  .testi-card{ background:var(--surface); border:1px solid var(--line); border-radius:var(--radius); padding:28px; }
  .testi-stars{ color:var(--brass-bright); font-size:14px; margin-bottom:14px; letter-spacing:2px; }
  .testi-card p.quote{ font-size:14.5px; color:var(--text-dim); margin-bottom:20px; line-height:1.65; }
  .testi-who{ display:flex; align-items:center; gap:12px; }
  .testi-avatar{ width:38px; height:38px; border-radius:50%; background:var(--brass-dim); color:var(--brass-bright); display:flex; align-items:center; justify-content:center; font-family:'IBM Plex Mono',monospace; font-size:12px; font-weight:600; }
  .testi-who .name{ font-size:14px; font-weight:600; }
  .testi-who .role{ font-size:12px; color:var(--text-faint); }

  /* FAQ */
  .faq-list{ border-top:1px solid var(--line); max-width:760px; }
  .faq-item{ border-bottom:1px solid var(--line); }
  .faq-q{ width:100%; display:flex; align-items:center; justify-content:space-between; padding:22px 4px; font-size:15.5px; font-weight:500; text-align:left; }
  .faq-q .plus{ width:18px; height:18px; position:relative; flex-shrink:0; margin-left:20px; }
  .faq-q .plus::before, .faq-q .plus::after{ content:""; position:absolute; background:var(--brass-bright); border-radius:2px; }
  .faq-q .plus::before{ width:100%; height:2px; top:50%; left:0; transform:translateY(-50%); }
  .faq-q .plus::after{ width:2px; height:100%; left:50%; top:0; transform:translateX(-50%); transition:transform .25s ease, opacity .25s ease; }
  .faq-item.open .plus::after{ transform:translateX(-50%) rotate(90deg); opacity:0; }
  .faq-a{ max-height:0; overflow:hidden; transition:max-height .3s ease; }
  .faq-a p{ padding:0 4px 22px; color:var(--text-dim); font-size:14px; max-width:640px; }

  /* Final CTA */
  .cta-band{ text-align:center; padding:96px 0 116px; }
  .cta-band h2{ font-size:clamp(2rem, 4vw, 2.8rem); margin-bottom:16px; }
  .cta-band p{ color:var(--text-dim); max-width:460px; margin:0 auto 32px; }
  .cta-actions{ display:flex; gap:14px; justify-content:center; flex-wrap:wrap; }

  /* Footer */
  footer{ border-top:1px solid var(--line); padding:52px 0 36px; }
  .footer-grid{ display:flex; justify-content:space-between; flex-wrap:wrap; gap:40px; margin-bottom:44px; }
  .footer-brand p{ color:var(--text-faint); font-size:13.5px; margin-top:12px; max-width:250px; }
  .footer-cols{ display:flex; gap:60px; flex-wrap:wrap; }
  .footer-col h4{ font-family:'IBM Plex Mono',monospace; font-size:11px; letter-spacing:0.07em; color:var(--text-faint); text-transform:uppercase; margin-bottom:16px; font-weight:500; }
  .footer-col a{ display:block; color:var(--text-dim); font-size:13.5px; margin-bottom:11px; transition:color .2s ease; }
  .footer-col a:hover{ color:var(--brass-bright); }
  .footer-bottom{ display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:16px; padding-top:26px; border-top:1px solid var(--line); color:var(--text-faint); font-size:12.5px; }
</style>
</head>
<body>

<div class="aurora"><span class="a1"></span><span class="a2"></span></div>

<header>
  <div class="wrap">
    <nav>
      <a href="#" class="brand">
        <svg class="brand-mark" viewBox="0 0 32 32" fill="none">
          <path d="M16 2 L28 7 V16 C28 23 22.5 28 16 30 C9.5 28 4 23 4 16 V7 Z" stroke="#c9a227" stroke-width="1.6" fill="rgba(201,162,39,0.08)"/>
          <path d="M12 16.5l3 3 6-6.5" stroke="#e8c860" stroke-width="1.8" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        RapiQR
      </a>
      <div class="nav-links">
        <a href="#products">Products</a>
        <a href="#how">How it works</a>
        <a href="#usecases">Use cases</a>
        <a href="#pricing">Pricing</a>
        <a href="#distributor">Partner</a>
        <a href="#faq">FAQ</a>
      </div>
      <div class="nav-actions">
        <a href="#demo" class="btn btn-ghost btn-sm">See it in action</a>
        <a href="#products" class="btn btn-primary btn-sm">Get My Sticker</a>
      </div>
    </nav>
  </div>
</header>

<main>
  <section class="hero">
    <div class="wrap hero-grid">
      <div>
        <div class="eyebrow">India's #1st emergency platform</div>
        <h1>Instant QR safety for your <em>family, vehicles &amp; home.</em></h1>
        <p class="lead">No app needed. Zero subscription. Anyone who scans your RapiQR tag can reach you, share their location, or trigger an emergency alert — without ever seeing your number.</p>
        <div class="hero-ctas">
          <a href="#products" class="btn btn-primary">Get My Sticker</a>
          <a href="#demo" class="btn btn-ghost">See It In Action</a>
        </div>
        <div class="trust-row">
          <div class="avatars"><span>RM</span><span>PS</span><span>KR</span><span>AM</span></div>
          <p>Trusted by <strong>10,000+</strong> vehicle &amp; home owners</p>
        </div>
      </div>

      <div class="hero-visual" id="heroVisual">
        <div class="badge-float speed">⚡ Alert speed <span>&lt;1.8s</span></div>
        <div class="sticker-card">
          <div class="sticker-top">
            <div class="sticker-brand">
              <svg viewBox="0 0 32 32" fill="none"><path d="M16 2 L28 7 V16 C28 23 22.5 28 16 30 C9.5 28 4 23 4 16 V7 Z" stroke="#12100a" stroke-width="2" fill="none"/></svg>
              RapiQR Safe Tag
            </div>
          </div>
          <div class="sticker-tag">SAFETY STICKER</div>
          <div class="sticker-qr" style="margin-top:12px;" id="heroQr"></div>
          <div class="sticker-plate">
            <div class="id">KA-01-MJ-9921</div>
            <div class="sub">OUTDOOR WEATHERPROOF TAG</div>
          </div>
        </div>

        <div class="phone-mockup">
          <div class="phone-notch"></div>
          <div class="phone-label">Real scanner browser view</div>
          <div class="phone-screen">
            <div class="car">Car KA-01-MJ-9921</div>
            <div class="status">● Owner registered · Private mode</div>
            <div class="phone-btn">📞 Call Owner (Private Proxy)</div>
            <div class="phone-btn">✉️ Send Message</div>
            <div class="phone-btn">📍 Share GPS Location</div>
            <div class="phone-btn alert">🚨 Emergency SOS</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="stats-band">
    <div class="wrap">
      <div class="stats-grid">
        <div class="stat-cell reveal"><div class="icon">🛡️</div><div class="num" data-count="10000" data-suffix="+">0</div><div class="label">Families protected</div></div>
        <div class="stat-cell reveal"><div class="icon">⚡</div><div class="num" data-static="&lt;3 sec">0</div><div class="label">Alert delivery</div></div>
        <div class="stat-cell reveal"><div class="icon">🚨</div><div class="num" data-count="13" data-suffix="">0</div><div class="label">Emergency types</div></div>
      </div>
    </div>
  </div>

  <section id="products">
    <div class="wrap">
      <div class="section-head">
        <div class="eyebrow">Safety tags</div>
        <h2>One QR. Lifetime protection.</h2>
        <p>Buy once. Stick it. Done. Six tags, one platform — pick the ones that match your family.</p>
      </div>
      <div class="filter-row" id="filterRow">
        <button class="filter-pill active" data-filter="all">All Products</button>
        <button class="filter-pill" data-filter="vehicle">Vehicle</button>
        <button class="filter-pill" data-filter="home">Home</button>
        <button class="filter-pill" data-filter="family">Family</button>
        <button class="filter-pill" data-filter="travel">Travel</button>
      </div>

      <div class="product-grid" id="productGrid">
        <div class="product-card" data-category="vehicle">
          <div class="product-top"><span class="product-badge">Best Seller</span><span class="product-rating"><span class="stars">★★★★★</span> 4.9 (3,420)</span></div>
          <h3>Automobile Safety Tag</h3>
          <p class="desc">Wrong-parking alerts, vehicle-blocking pings, and crash SOS — all without giving your number to anyone.</p>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Wrong parking &amp; obstruction alerts</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Masked call routing — number never shown</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Crash SOS at 40G deceleration</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Weatherproof 3M polycarbonate, 3+ years</li>
          </ul>
          <div class="product-price-row"><span class="now">₹349</span><span class="was">₹499</span><span class="off">30% OFF</span></div>
          <div class="lifetime">Lifetime · ₹0 subscription</div>
          <a href="#pricing" class="btn btn-primary btn-block">Buy Now</a>
        </div>

        <div class="product-card" data-category="vehicle">
          <div class="product-top"><span class="product-badge">Popular</span><span class="product-rating"><span class="stars">★★★★★</span> 4.8 (2,150)</span></div>
          <h3>Motorcycle &amp; Helmet Tag</h3>
          <p class="desc">Anti-tamper movement alerts, first-responder medical access, and insurance reminders for riders.</p>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Emergency hotline for first responders</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Blood group &amp; allergy medical card</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Insurance &amp; PUC service reminders</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Scratch-resistant, fits helmet or tank</li>
          </ul>
          <div class="product-price-row"><span class="now">₹249</span><span class="was">₹399</span><span class="off">38% OFF</span></div>
          <div class="lifetime">Lifetime · ₹0 subscription</div>
          <a href="#pricing" class="btn btn-primary btn-block">Buy Now</a>
        </div>

        <div class="product-card" data-category="home">
          <div class="product-top"><span class="product-badge">Smart Home</span><span class="product-rating"><span class="stars">★★★★★</span> 4.9 (1,890)</span></div>
          <h3>Residential Gate Tag</h3>
          <p class="desc">Courier drop-off pings, visitor check-in, and hazard reports — without publishing your number at your gate.</p>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Courier drop-off WhatsApp pings</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Visitor check-in without number exposure</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Hazard &amp; pipe leak reporting by neighbours</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Heavy-duty weatherproof acrylic mount</li>
          </ul>
          <div class="product-price-row"><span class="now">₹349</span><span class="was">₹499</span><span class="off">30% OFF</span></div>
          <div class="lifetime">Lifetime · ₹0 subscription</div>
          <a href="#pricing" class="btn btn-primary btn-block">Buy Now</a>
        </div>

        <div class="product-card" data-category="family">
          <div class="product-top"><span class="product-badge">Kids Safety</span><span class="product-rating"><span class="stars">★★★★★</span> 5.0 (1,420)</span></div>
          <h3>Pediatric School Bag Tag</h3>
          <p class="desc">Emergency medical card, blood group, allergy info, and masked parent hotline — for when a stranger finds your child.</p>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Blood group &amp; allergy directives visible to finder</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>School bus pickup &amp; drop-off alerts</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Encrypted guardian contact for school staff</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Child-safe non-toxic TPU clip</li>
          </ul>
          <div class="product-price-row"><span class="now">₹249</span><span class="was">₹349</span><span class="off">29% OFF</span></div>
          <div class="lifetime">Lifetime · ₹0 subscription</div>
          <a href="#pricing" class="btn btn-primary btn-block">Buy Now</a>
        </div>

        <div class="product-card" data-category="family">
          <div class="product-top"><span class="product-badge">Senior Care</span><span class="product-rating"><span class="stars">★★★★★</span> 4.9 (1,650)</span></div>
          <h3>Senior Medical Keychain</h3>
          <p class="desc">Vital health record access and one-tap guardian alert — essential for elderly family members out alone.</p>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Medical notes &amp; doctor contacts for responders</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Masked phone proxy connects finder to family</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Designed for elderly parents on walks or travel</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Ultra-light anodised aircraft aluminium case</li>
          </ul>
          <div class="product-price-row"><span class="now">₹249</span><span class="was">₹349</span><span class="off">29% OFF</span></div>
          <div class="lifetime">Lifetime · ₹0 subscription</div>
          <a href="#pricing" class="btn btn-primary btn-block">Buy Now</a>
        </div>

        <div class="product-card" data-category="travel">
          <div class="product-top"><span class="product-badge">Travel Essential</span><span class="product-rating"><span class="stars">★★★★★</span> 4.8 (980)</span></div>
          <h3>Smart Luggage Tag</h3>
          <p class="desc">Instant scan-notification with GPS pin to your phone — and anonymous finder messaging, no address exposed.</p>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Anonymous finder messaging — no address shown</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Instant SMS &amp; WhatsApp scan notification</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Works across airlines, transit hubs, globally</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M20 6L9 17l-5-5"/></svg>Braided stainless steel cable included</li>
          </ul>
          <div class="product-price-row"><span class="now">₹249</span><span class="was">₹349</span><span class="off">29% OFF</span></div>
          <div class="lifetime">Lifetime · ₹0 subscription</div>
          <a href="#pricing" class="btn btn-primary btn-block">Buy Now</a>
        </div>
      </div>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <section id="how">
    <div class="wrap">
      <div class="section-head">
        <div class="eyebrow">How it works</div>
        <h2>Five steps. Done.</h2>
        <p>Simple, instant, and privacy-protected from start to finish.</p>
      </div>
      <div class="steps-row">
        <div class="step"><span class="idx">1</span><h3>Order your sticker</h3><p>Pick a type — vehicle, gate, bag, keychain. Ships in 2–3 days.</p></div>
        <div class="step"><span class="idx">2</span><h3>Stick it on</h3><p>Weatherproof 3M adhesive. Rated 3+ years outdoors.</p></div>
        <div class="step active"><span class="idx">3</span><h3>Anyone scans — no app</h3><p>Camera → secure page. Nothing to install.</p><span class="live">● Active step</span></div>
        <div class="step"><span class="idx">4</span><h3>They reach you</h3><p>Call, message, GPS, or alert. Your number? Never visible.</p></div>
        <div class="step"><span class="idx">5</span><h3>You get pinged instantly</h3><p>Push + SMS + WhatsApp. Resolve in one tap.</p></div>
      </div>
    </div>
  </section>

  <section id="usecases">
    <div class="wrap">
      <div class="section-head">
        <div class="eyebrow">Use cases</div>
        <h2>One QR. Every situation.</h2>
      </div>
      <div class="usecase-tabs" id="usecaseTabs">
        <button class="usecase-tab active" data-tab="vehicles">Vehicles</button>
        <button class="usecase-tab" data-tab="gate">Home Gate</button>
        <button class="usecase-tab" data-tab="luggage">Luggage</button>
        <button class="usecase-tab" data-tab="keychain">Keychains</button>
        <button class="usecase-tab" data-tab="family">Family</button>
      </div>

      <div class="usecase-panel active" data-panel="vehicles">
        <div>
          <h3>Visible to strangers. Your number doesn't have to be.</h3>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Parking alert? Get it in seconds — no windshield note.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Crash SOS connects responders to family, not voicemail.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Blocked driveway? WhatsApp alert before it turns ugly.</li>
          </ul>
          <a href="#pricing" class="btn btn-ghost">Get Vehicles Tag</a>
        </div>
        <div class="usecase-visual"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M3 12l2-7h14l2 7"/><path d="M5 12v7a1 1 0 0 0 1 1h1a1 1 0 0 0 1-1v-1h8v1a1 1 0 0 0 1 1h1a1 1 0 0 0 1-1v-7"/><circle cx="7.5" cy="15.5" r="1"/><circle cx="16.5" cy="15.5" r="1"/></svg></div>
      </div>

      <div class="usecase-panel" data-panel="gate">
        <div>
          <h3>Let people reach you. Keep your number off the gate.</h3>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Courier arrives? They scan and ping you instantly.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Visitors check in. You decide if you open the gate.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Neighbour spots a leak? Alert hits your phone in 3 sec.</li>
          </ul>
          <a href="#pricing" class="btn btn-ghost">Get Home Gate Tag</a>
        </div>
        <div class="usecase-visual"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M3 10l9-6 9 6"/><path d="M5 9v11a1 1 0 0 0 1 1h4v-6h4v6h4a1 1 0 0 0 1-1V9"/></svg></div>
      </div>

      <div class="usecase-panel" data-panel="luggage">
        <div>
          <h3>Lost bag. Found. Returned. No address on the label.</h3>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Finder scans → messages you. Your address stays private.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>You get a GPS pin of where the scan happened.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Airport, hotel, train station. No app for the finder.</li>
          </ul>
          <a href="#pricing" class="btn btn-ghost">Get Luggage Tag</a>
        </div>
        <div class="usecase-visual"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><rect x="6" y="7" width="12" height="14" rx="2"/><path d="M9 7V5a3 3 0 0 1 6 0v2"/></svg></div>
      </div>

      <div class="usecase-panel" data-panel="keychain">
        <div>
          <h3>Found keys. Two-minute return. No stress.</h3>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Finder scans, shares location, you arrange pickup.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Medical ID for seniors: blood group &amp; contacts — opt-in.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Weatherproof. Clips to keys, bag straps, walking aids.</li>
          </ul>
          <a href="#pricing" class="btn btn-ghost">Get Keychains Tag</a>
        </div>
        <div class="usecase-visual"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><circle cx="8" cy="8" r="4"/><path d="M11 11l9 9M16 16l3-3M18 18l3-3"/></svg></div>
      </div>

      <div class="usecase-panel" data-panel="family">
        <div>
          <h3>Safety net for kids, seniors, and pets. No phone needed.</h3>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>School bag tag → teacher messages you instantly.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Senior keychain shows blood group in an emergency.</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Pet found? Finder calls you via masked number. Done.</li>
          </ul>
          <a href="#pricing" class="btn btn-ghost">Get Family Tag</a>
        </div>
        <div class="usecase-visual"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><circle cx="9" cy="7" r="3"/><circle cx="17" cy="8" r="2.4"/><path d="M3 21v-2a5 5 0 0 1 5-5h2a5 5 0 0 1 5 5v2"/><path d="M15.5 12.5A4 4 0 0 1 21 16.2V18"/></svg></div>
      </div>
    </div>
  </section>

  <section id="inside">
    <div class="wrap">
      <div class="section-head">
        <div class="eyebrow">What's inside</div>
        <h2>Built to answer every doubt.</h2>
      </div>
      <div class="feature-grid">
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M12 2l8 3.5v6c0 5.5-3.4 9-8 10.5-4.6-1.5-8-5-8-10.5v-6z"/></svg><h3>Zero number exposure</h3><p>Your real number is never shown. Not stored. Not transmitted. Ever.</p></div>
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M12 2v20M2 12h20"/><circle cx="12" cy="12" r="9"/></svg><h3>Live GPS sharing</h3><p>Scanners share their exact location in one tap.</p></div>
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M22 16.9v3a2 2 0 0 1-2.2 2 19.8 19.8 0 0 1-8.6-3 19.5 19.5 0 0 1-6-6 19.8 19.8 0 0 1-3-8.7A2 2 0 0 1 4.1 2h3a2 2 0 0 1 2 1.7c.1 1 .3 2 .7 2.9a2 2 0 0 1-.5 2.1L8 10a16 16 0 0 0 6 6l1.3-1.3a2 2 0 0 1 2.1-.5c.9.4 1.9.6 2.9.7a2 2 0 0 1 1.7 2z"/></svg><h3>Multi-channel alerts</h3><p>Push + SMS + WhatsApp. All at once.</p></div>
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="5" y="2" width="14" height="20" rx="2"/><path d="M12 18h.01"/></svg><h3>No app for scanners</h3><p>Camera → page. No download. No login.</p></div>
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M12 9v4M12 17h.01"/><path d="M10.3 3.9L2.5 17a1.8 1.8 0 0 0 1.6 2.7h15.8a1.8 1.8 0 0 0 1.6-2.7L13.7 3.9a1.8 1.8 0 0 0-3.4 0z"/></svg><h3>13 emergency types</h3><p>Parking, crash, fire, medical, lost child, and more.</p></div>
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="4" y="10" width="16" height="10" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/></svg><h3>Encrypted &amp; access-controlled</h3><p>Each sticker has rules. Only the right people see the right info.</p></div>
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M20.8 4.6c-1.9-1.9-5-1.9-6.9 0L12 6.5l-1.9-1.9c-1.9-1.9-5-1.9-6.9 0-1.9 1.9-1.9 5 0 6.9L12 20.3l8.8-8.8c1.9-1.9 1.9-5 0-6.9z"/></svg><h3>Opt-in medical profile</h3><p>Blood group, allergies, SOS contacts — only on tags you choose.</p></div>
        <div class="feature"><svg class="glyph" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M4 4v6h6M20 20v-6h-6"/><path d="M4.5 15a8 8 0 0 0 14.6 3M19.5 9A8 8 0 0 0 4.9 6"/></svg><h3>Instant sticker restore</h3><p>Lost sticker? Reprint. Same ID. Same settings. Zero cost.</p></div>
      </div>
    </div>
  </section>

  <section id="demo">
    <div class="wrap">
      <div class="section-head">
        <div class="eyebrow">Try it now</div>
        <h2>What the scanner sees.</h2>
        <p>Tap through every action below. See exactly what a stranger can do — and what stays hidden from them.</p>
      </div>

      <div class="demo-shell">
        <div class="demo-panel">
          <div class="demo-stepper" id="demoStepper">
            <div class="dstep active" data-step="1">1. SCAN</div>
            <div class="dstep" data-step="2">2. CONTACT</div>
            <div class="dstep" data-step="3">3. SHARE GPS</div>
            <div class="dstep" data-step="4">4. EMERGENCY</div>
          </div>
          <h3 id="demoTitle">What the scanner sees</h3>
          <p id="demoDesc">This is the exact page a stranger sees after scanning your RapiQR tag — no app, no login. Tap an action on the phone to see what happens on your end.</p>
          <div class="demo-panel-actions">
            <button class="btn btn-ghost" id="demoRestart">Restart demo</button>
          </div>
        </div>

        <div class="demo-phone">
          <div class="notch"></div>
          <div class="demo-screen">

            <div class="demo-state active" data-state="initial">
              <div class="demo-tag-id">RapiQR Safe · Car Tag</div>
              <div class="demo-tag-sub">KA-01-MJ-9921 · Registered vehicle · Mumbai</div>
              <div class="demo-cta-label">TAP AN ACTION ↓</div>
              <button class="demo-action" data-go="call"><span class="ic">📞</span> Call Owner (Private)</button>
              <button class="demo-action" data-go="message"><span class="ic">✉️</span> Send a Message</button>
              <button class="demo-action" data-go="location"><span class="ic">📍</span> Share My Location</button>
              <button class="demo-action emergency" data-go="emergency"><span class="ic">🚨</span> Emergency Alert</button>
            </div>

            <div class="demo-state" data-state="call">
              <button class="demo-back" data-go="initial">← Back</button>
              <div class="result-icon">📞</div>
              <div class="result-title">Calling owner</div>
              <div class="result-sub">Connected via private proxy number</div>
              <div class="result-line"><span>📱</span><span><span class="rt">+91 98×× ××××XX</span><span class="rs">Masked proxy number · Your real number is never shared</span></span></div>
              <div class="result-line"><span>✅</span><span><span class="rt">Owner notified</span><span class="rs">Push + SMS delivered in 0.4 sec</span></span></div>
              <button class="btn btn-primary demo-next" data-go="location">Next: Share Location</button>
            </div>

            <div class="demo-state" data-state="message">
              <button class="demo-back" data-go="initial">← Back</button>
              <div class="result-icon">✉️</div>
              <div class="result-title">Message delivered</div>
              <div class="result-sub">Sent through the encrypted relay</div>
              <div class="result-line"><span>💬</span><span><span class="rt">Your note reached the owner</span><span class="rs">No contact card or number exchanged</span></span></div>
              <div class="result-line"><span>✅</span><span><span class="rt">Owner notified</span><span class="rs">Push + SMS + WhatsApp delivered</span></span></div>
              <button class="btn btn-primary demo-next" data-go="location">Next: Share Location</button>
            </div>

            <div class="demo-state" data-state="location">
              <button class="demo-back" data-go="initial">← Back</button>
              <div class="result-icon safe">📍</div>
              <div class="result-title">Location shared</div>
              <div class="result-sub">GPS pin sent to owner</div>
              <div class="result-line"><span>🗺️</span><span><span class="rt">Bandra West, Mumbai</span><span class="rs">Accurate to 5m</span></span></div>
              <div class="result-line"><span>📱</span><span><span class="rt">Owner sees your location</span><span class="rs">They can coordinate pickup in real time</span></span></div>
              <button class="btn btn-alert demo-next" data-go="emergency">See Emergency Flow</button>
            </div>

            <div class="demo-state" data-state="emergency">
              <button class="demo-back" data-go="initial">← Back</button>
              <div class="result-title">🚨 Emergency Alert</div>
              <div class="result-sub" style="margin-bottom:16px;">Select the type of emergency</div>
              <div class="emg-grid">
                <div class="emg-item">Accident / Collision</div>
                <div class="emg-item">Medical Emergency</div>
                <div class="emg-item">Fire / Gas Leak</div>
                <div class="emg-item">Wrong Parking</div>
              </div>
              <div class="emg-caption">Alert fires instantly · Owner + guardians notified</div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="pricing">
    <div class="wrap">
      <div class="section-head center">
        <div class="eyebrow">Pricing</div>
        <h2>Buy once. Done forever.</h2>
        <p>No monthly fees. No renewals.</p>
      </div>
      <div class="pricing-grid">
        <div class="price-card">
          <h3>Starter</h3>
          <div class="price-sub">One sticker, lifetime protection. Ideal for a single vehicle, gate, or bag.</div>
          <div class="amount">₹349<span>₹499</span></div>
          <div class="per">One-time · lifetime access</div>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>1 sticker of your choice</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Lifetime platform access</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Unlimited scan events</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>SMS + push notifications</li>
          </ul>
          <a href="#" class="btn btn-ghost btn-block">Get My Sticker</a>
        </div>
        <div class="price-card featured">
          <h3>Family Bundle</h3>
          <div class="price-sub">Cover a car, your home gate, and a child — the most common family setup.</div>
          <div class="amount">₹899<span>₹1,097</span></div>
          <div class="per">One-time · lifetime access</div>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Automobile Safety Tag</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Residential Gate Tag</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Pediatric School Bag Tag</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Opt-in medical profiles</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Multi-sticker dashboard</li>
          </ul>
          <a href="#" class="btn btn-primary btn-block">Protect My Family</a>
        </div>
        <div class="price-card">
          <h3>Society / Fleet</h3>
          <div class="price-sub">Housing societies and small fleets. Bulk pricing, central dashboard, priority support.</div>
          <div class="amount">Custom</div>
          <div class="per">Custom quote</div>
          <ul>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>10+ stickers, custom quantity</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Society admin dashboard</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Gate + vehicle management</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Dedicated onboarding call</li>
            <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Priority support SLA</li>
          </ul>
          <a href="#distributor" class="btn btn-ghost btn-block">Talk to Us</a>
        </div>
      </div>
    </div>
  </section>

  <section class="distributor-band" id="distributor">
    <div class="wrap" style="padding-top:104px; padding-bottom:104px;">
      <div class="section-head">
        <div class="eyebrow">Partner program</div>
        <h2>Become a RapiQR Distributor.</h2>
        <p>High margins, zero tech hassle, and instant product demand. Sell smart QR safety stickers in your shop, city, or dealer network.</p>
      </div>
      <div class="dist-benefits">
        <div class="dist-card"><div class="num">01</div><h4>50%+ Profit Margins</h4><p>High retail markup per sticker. Earn healthy profits from day one with bulk wholesale pricing.</p></div>
        <div class="dist-card"><div class="num">02</div><h4>Free POS Counter Display</h4><p>Includes attractive acrylic counter stands, promotional posters, and QR scan demos for your shop.</p></div>
        <div class="dist-card"><div class="num">03</div><h4>Partner Admin Portal</h4><p>Manage inventory, assign tags to customers in 5 seconds, and track reseller commissions live.</p></div>
        <div class="dist-card"><div class="num">04</div><h4>Territory Lock Rights</h4><p>Exclusive city/district dealership rights available for qualified regional distributors.</p></div>
      </div>
      <div class="kit-grid">
        <div class="kit-card">
          <div class="kit-tag">Retail Partner</div>
          <h3>Shopkeeper Starter Kit</h3>
          <div class="kit-sub">Ideal for car accessory shops, helmet stores, society gates &amp; local retailers.</div>
          <div class="kit-price mono">₹4,999 / 50 Stickers Kit</div>
          <ul>
            <li>✓ 50 Assorted Safety QR Stickers</li>
            <li>✓ Acrylic Tabletop Counter Stand Display</li>
            <li>✓ 45% Retail Profit Margin</li>
            <li>✓ Basic Partner Activation Access</li>
          </ul>
          <a href="#" class="btn btn-ghost btn-block">Apply for Retail Kit →</a>
        </div>
        <div class="kit-card highlight">
          <div class="kit-tag">Exclusive Franchise · High Income</div>
          <h3>City Master Distributor</h3>
          <div class="kit-sub">Exclusive supply rights for your entire city or district.</div>
          <div class="kit-price mono">₹24,999 / 300 Stickers + Exclusive Lock</div>
          <ul>
            <li>✓ 300 QR Stickers + City Exclusive Rights</li>
            <li>✓ 5 Counter Stands + Marketing Banners</li>
            <li>✓ 60% Profit Margin + Sub-dealer Admin</li>
            <li>✓ Dedicated Relationship Manager &amp; Leads</li>
          </ul>
          <a href="#" class="btn btn-primary btn-block">Apply for City Distributorship →</a>
        </div>
      </div>
      <div class="dist-contact">Questions? Call our B2B Partner Desk directly at <a href="tel:+919876543210">+91 98765 43210</a> or email <a href="mailto:b2b@rapiqr.com">b2b@rapiqr.com</a></div>
    </div>
  </section>

  <section>
    <div class="wrap">
      <div class="section-head">
        <div class="eyebrow">What people say</div>
        <h2>Real people. Real scans.</h2>
      </div>
      <div class="testi-note">↓ Sample testimonials — replace with real ones before launch</div>
      <div class="testi-grid">
        <div class="testi-card reveal">
          <div class="testi-stars">★★★★★</div>
          <p class="quote">"A traffic officer scanned my tag when I was double-parked. I got an alert and moved my car in 4 minutes. He never saw my number. This is exactly what I needed."</p>
          <div class="testi-who"><div class="testi-avatar">RM</div><div><div class="name">Rajesh M.</div><div class="role">Car Owner, Pune</div></div></div>
        </div>
        <div class="testi-card reveal">
          <div class="testi-stars">★★★★★</div>
          <p class="quote">"My son's school bag has the tag. When he was on the wrong bus, his teacher scanned it and messaged me instantly — without me having published my number to 30 strangers."</p>
          <div class="testi-who"><div class="testi-avatar">PS</div><div><div class="name">Priya S.</div><div class="role">Parent of two, Bengaluru</div></div></div>
        </div>
        <div class="testi-card reveal">
          <div class="testi-stars">★★★★★</div>
          <p class="quote">"Lost my bag at Hyderabad airport. Got a scan notification with GPS in under a minute. The airline staff found it three hours later. First time in 11 years I got a bag back."</p>
          <div class="testi-who"><div class="testi-avatar">AT</div><div><div class="name">Arun T.</div><div class="role">Frequent traveller, Mumbai</div></div></div>
        </div>
        <div class="testi-card reveal">
          <div class="testi-stars">★★★★★</div>
          <p class="quote">"We put gate tags on our entrance pillars. Now couriers ping residents instead of buzzing the intercom all day. Complaints dropped immediately. Residents love it."</p>
          <div class="testi-who"><div class="testi-avatar">KR</div><div><div class="name">Kavitha R.</div><div class="role">Housing Society Secretary, Chennai</div></div></div>
        </div>
      </div>
    </div>
  </section>

  <section id="faq">
    <div class="wrap">
      <div class="section-head">
        <div class="eyebrow">FAQ</div>
        <h2>Quick answers.</h2>
      </div>
      <div class="faq-list">
        <div class="faq-item">
          <button class="faq-q"><span>Does the scanner need to download an app?</span><span class="plus"></span></button>
          <div class="faq-a"><p>No. Any phone camera opens a secure browser page directly — nothing to install, nothing to sign up for. RapiQR only has an app-like dashboard for you, the owner, never for the person scanning your tag.</p></div>
        </div>
        <div class="faq-item">
          <button class="faq-q"><span>Will the scanner ever see my real phone number?</span><span class="plus"></span></button>
          <div class="faq-a"><p>Never. Every call and message is routed through a private proxy number. Your real number is not shown, not stored, and not transmitted at any point in the flow.</p></div>
        </div>
        <div class="faq-item">
          <button class="faq-q"><span>What if my sticker is lost or damaged?</span><span class="plus"></span></button>
          <div class="faq-a"><p>Reprint it from your dashboard. You get the same ID and the same settings at zero extra cost — your protection doesn't reset just because the sticker did.</p></div>
        </div>
        <div class="faq-item">
          <button class="faq-q"><span>Is my medical or family data ever shown to anyone?</span><span class="plus"></span></button>
          <div class="faq-a"><p>Only what you opt in to, and only to the extent you allow. Medical profiles, blood group, and emergency contacts are access-controlled per tag — you choose what a finder can see.</p></div>
        </div>
        <div class="faq-item">
          <button class="faq-q"><span>Is there a subscription or renewal fee?</span><span class="plus"></span></button>
          <div class="faq-a"><p>No. It's a one-time purchase with lifetime platform access. No monthly fees, no renewals, nothing that expires.</p></div>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-band">
    <div class="wrap">
      <div class="eyebrow" style="justify-content:center;">One-time purchase · Lifetime protection</div>
      <h2>Protect everyone you care for.<br>Keep your number private.</h2>
      <p>Stick it on. Scan happens. You're protected.</p>
      <div class="cta-actions">
        <a href="#products" class="btn btn-primary">Get My Sticker</a>
        <a href="#demo" class="btn btn-ghost">Try the Demo First</a>
      </div>
    </div>
  </section>
</main>

<footer>
  <div class="wrap">
    <div class="footer-grid">
      <div class="footer-brand">
        <a href="#" class="brand">
          <svg class="brand-mark" viewBox="0 0 32 32" fill="none">
            <path d="M16 2 L28 7 V16 C28 23 22.5 28 16 30 C9.5 28 4 23 4 16 V7 Z" stroke="#c9a227" stroke-width="1.6" fill="rgba(201,162,39,0.08)"/>
            <path d="M12 16.5l3 3 6-6.5" stroke="#e8c860" stroke-width="1.8" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          RapiQR
        </a>
        <p>One QR. Lifetime protection.</p>
      </div>
      <div class="footer-cols">
        <div class="footer-col">
          <h4>Product</h4>
          <a href="#how">How It Works</a>
          <a href="#products">Vehicle Tags</a>
          <a href="#products">Home Gate Tags</a>
          <a href="#products">Family Tags</a>
          <a href="#products">Luggage Tags</a>
        </div>
        <div class="footer-col">
          <h4>Company</h4>
          <a href="#">About</a>
          <a href="#distributor">Partner Program</a>
          <a href="#">Privacy Policy</a>
          <a href="#">Terms of Service</a>
          <a href="#">Refund Policy</a>
        </div>
        <div class="footer-col">
          <h4>Support</h4>
          <a href="#">Contact Support</a>
          <a href="#">Help Centre</a>
          <a href="#faq">FAQ</a>
          <a href="#">Track Your Order</a>
          <a href="#">Replace a Sticker</a>
          <a href="#distributor">Enterprise / Fleet</a>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© 2026 RapiQR. All rights reserved.</span>
      <span class="mono">MADE FOR EVERY SCAN</span>
    </div>
  </div>
</footer>

<script>
  /* Real scannable QR code in the hero sticker */
  if (window.QRCode) {
    new QRCode(document.getElementById("heroQr"), {
      text: "https://rapiqr.com/t/KA-01-MJ-9921",
      width: 210,
      height: 210,
      colorDark: "#0a0b0e",
      colorLight: "#ffffff",
      correctLevel: QRCode.CorrectLevel.H
    });
  }

  /* Smooth anchor scroll */
  document.querySelectorAll('a[href^="#"]').forEach(function(link){
    link.addEventListener('click', function(e){
      var id = this.getAttribute('href');
      if(id.length > 1){
        var target = document.querySelector(id);
        if(target){ e.preventDefault(); target.scrollIntoView({behavior:'smooth', block:'start'}); }
      }
    });
  });

  /* Reveal on scroll */
  if('IntersectionObserver' in window){
    var io = new IntersectionObserver(function(entries){
      entries.forEach(function(entry){
        if(entry.isIntersecting){ entry.target.classList.add('in'); io.unobserve(entry.target); }
      });
    }, { threshold: 0.15 });
    document.querySelectorAll('.reveal').forEach(function(el){ io.observe(el); });
  } else {
    document.querySelectorAll('.reveal').forEach(function(el){ el.classList.add('in'); });
  }

  /* Animated stat counters */
  function animateCount(el){
    var target = parseInt(el.getAttribute('data-count'), 10);
    var suffix = el.getAttribute('data-suffix') || '';
    var staticVal = el.getAttribute('data-static');
    if(staticVal){ el.textContent = staticVal.replace('&lt;','<'); return; }
    var start = 0, duration = 1400, startTime = null;
    function step(ts){
      if(!startTime) startTime = ts;
      var progress = Math.min((ts - startTime) / duration, 1);
      var eased = 1 - Math.pow(1 - progress, 3);
      el.textContent = Math.floor(eased * target).toLocaleString('en-IN') + suffix;
      if(progress < 1) requestAnimationFrame(step);
      else el.textContent = target.toLocaleString('en-IN') + suffix;
    }
    requestAnimationFrame(step);
  }
  if('IntersectionObserver' in window){
    var statIo = new IntersectionObserver(function(entries){
      entries.forEach(function(entry){
        if(entry.isIntersecting){
          var num = entry.target.querySelector('.num');
          if(num) animateCount(num);
          statIo.unobserve(entry.target);
        }
      });
    }, { threshold: 0.4 });
    document.querySelectorAll('.stat-cell').forEach(function(el){ statIo.observe(el); });
  }

  /* FAQ accordion */
  document.querySelectorAll('.faq-item').forEach(function(item){
    var btn = item.querySelector('.faq-q');
    var ans = item.querySelector('.faq-a');
    btn.addEventListener('click', function(){
      var isOpen = item.classList.contains('open');
      document.querySelectorAll('.faq-item.open').forEach(function(openItem){
        if(openItem !== item){ openItem.classList.remove('open'); openItem.querySelector('.faq-a').style.maxHeight = null; }
      });
      if(isOpen){ item.classList.remove('open'); ans.style.maxHeight = null; }
      else { item.classList.add('open'); ans.style.maxHeight = ans.scrollHeight + 'px'; }
    });
  });

  /* Product filter */
  var filterRow = document.getElementById('filterRow');
  filterRow.addEventListener('click', function(e){
    var btn = e.target.closest('.filter-pill');
    if(!btn) return;
    filterRow.querySelectorAll('.filter-pill').forEach(function(p){ p.classList.remove('active'); });
    btn.classList.add('active');
    var filter = btn.getAttribute('data-filter');
    document.querySelectorAll('#productGrid .product-card').forEach(function(card){
      var match = filter === 'all' || card.getAttribute('data-category') === filter;
      card.classList.toggle('hide', !match);
    });
  });

  /* Use-case tabs */
  var usecaseTabs = document.getElementById('usecaseTabs');
  usecaseTabs.addEventListener('click', function(e){
    var tab = e.target.closest('.usecase-tab');
    if(!tab) return;
    usecaseTabs.querySelectorAll('.usecase-tab').forEach(function(t){ t.classList.remove('active'); });
    tab.classList.add('active');
    var key = tab.getAttribute('data-tab');
    document.querySelectorAll('.usecase-panel').forEach(function(p){
      p.classList.toggle('active', p.getAttribute('data-panel') === key);
    });
  });

  /* Interactive scanner demo */
  var demoConfig = {
    initial: { title:'What the scanner sees', desc:'This is the exact page a stranger sees after scanning your RapiQR tag — no app, no login. Tap an action on the phone to see what happens on your end.', step:1 },
    call:    { title:'Your number stays hidden', desc:'The scanner connects through a masked proxy number. It rings your phone, but your real number is never revealed — not even in the call log.', step:2 },
    message: { title:'A message, not your number', desc:'Scanners can send you a note directly. It lands in your RapiQR inbox and forwards to your phone — no contact card exchanged.', step:2 },
    location:{ title:'Your location, sent precisely', desc:'The scanner shares their exact GPS location in one tap. You receive a live pin via push, SMS, and WhatsApp — useful for lost luggage, parked cars, or anything that needs a pickup.', step:3 },
    emergency:{ title:'Emergency, handled fast', desc:'One tap alerts you and your emergency contacts instantly, with the type of emergency attached so everyone knows what they’re walking into.', step:4 }
  };
  var demoScreen = document.querySelector('.demo-screen');
  var demoTitle = document.getElementById('demoTitle');
  var demoDesc = document.getElementById('demoDesc');
  var demoStepper = document.getElementById('demoStepper');

  function goToDemoState(state){
    var cfg = demoConfig[state];
    if(!cfg) return;
    document.querySelectorAll('.demo-state').forEach(function(s){ s.classList.toggle('active', s.getAttribute('data-state') === state); });
    demoTitle.style.opacity = 0; demoDesc.style.opacity = 0;
    setTimeout(function(){
      demoTitle.textContent = cfg.title;
      demoDesc.textContent = cfg.desc;
      demoTitle.style.opacity = 1; demoDesc.style.opacity = 1;
    }, 150);
    demoStepper.querySelectorAll('.dstep').forEach(function(d){
      d.classList.toggle('active', parseInt(d.getAttribute('data-step'),10) === cfg.step);
    });
  }
  demoScreen.addEventListener('click', function(e){
    var trigger = e.target.closest('[data-go]');
    if(!trigger) return;
    goToDemoState(trigger.getAttribute('data-go'));
  });
  document.getElementById('demoRestart').addEventListener('click', function(){ goToDemoState('initial'); });

  /* Subtle tilt on hero visual (desktop only) */
  var heroVisual = document.getElementById('heroVisual');
  if(window.matchMedia('(min-width: 981px)').matches && window.matchMedia('(hover: hover)').matches){
    heroVisual.addEventListener('mousemove', function(e){
      var rect = heroVisual.getBoundingClientRect();
      var x = (e.clientX - rect.left) / rect.width - 0.5;
      var y = (e.clientY - rect.top) / rect.height - 0.5;
      var sticker = heroVisual.querySelector('.sticker-card');
      var phone = heroVisual.querySelector('.phone-mockup');
      if(sticker) sticker.style.transform = 'rotate(' + (-7 - x*4) + 'deg) translateX(-30px) translateY(' + (y*8) + 'px)';
      if(phone) phone.style.transform = 'rotate(' + (6 - x*4) + 'deg) translateX(20px) translateY(' + (y*8) + 'px)';
    });
    heroVisual.addEventListener('mouseleave', function(){
      var sticker = heroVisual.querySelector('.sticker-card');
      var phone = heroVisual.querySelector('.phone-mockup');
      if(sticker) sticker.style.transform = '';
      if(phone) phone.style.transform = '';
    });
  }
</script>

</body>
</html>